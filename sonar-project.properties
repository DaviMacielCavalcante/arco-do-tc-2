# SonarQube Cloud — https://sonarcloud.io
#
# IMPORTANTE: ajuste `sonar.projectKey` e `sonar.organization` para EXATAMENTE
# os valores que a SonarQube Cloud mostra ao importar o projeto (os abaixo são
# o palpite padrão a partir do login do GitHub; confira em
# "Project > Information" / "Administration" no site). Se não baterem, a análise
# falha com "project not found".
sonar.projectKey=DaviMacielCavalcante_arco-do-tc-2
sonar.organization=davimacielcavalcante

sonar.projectName=arco-do-tc-2

# Código de produção e testes.
sonar.sources=src
sonar.tests=tests

# Python e cobertura (gerada no job `sonar` do CI via pytest-cov).
sonar.python.version=3.12
sonar.python.coverage.reportPaths=coverage.xml
